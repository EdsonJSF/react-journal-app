export const startNewNote = () => {
  return async (dispatch) => {
    // uid

    const newNote = {
      title: "",
      body: "",
      date: new Date().getTime(),
      imageUrls: [],
    };

    // TODO: Create dispatch
    // dispatch (newNote)
    // dispatch (activeNote)
  };
};
